# Workout prescription archive

This directory stores public, validated historical `current-workout.js` prescriptions only. Never add session handoffs, audio recordings, symptoms, or other private workout data here.

Before replacing the current prescription, validate the candidate, copy the current file as `YYYY-MM-DD--WORKOUT-ID.js`, replace it, and validate again.
